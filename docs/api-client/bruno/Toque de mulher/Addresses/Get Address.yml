info:
  name: Get Address
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/addresses/"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
