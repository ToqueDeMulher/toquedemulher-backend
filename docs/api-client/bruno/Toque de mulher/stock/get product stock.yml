info:
  name: get product stock
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/stock/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
