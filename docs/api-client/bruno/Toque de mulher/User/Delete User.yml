info:
  name: Delete User
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/user/me"
  body:
    type: json
    data: |-
      {
        "current_password": "123456",
        "confirm_text": "DELETE"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
